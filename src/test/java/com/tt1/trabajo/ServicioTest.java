package com.tt1.trabajo;

import modelo.Entidad;
import org.junit.jupiter.api.Test;
import servicios.Servicio;

import java.util.List;

import static org.junit.jupiter.api.Assertions.*;

class ServicioTest {

    /**
     * @Test
     *
     *     void testEntidadesTienenDatosCompletos() {
     *         Servicio servicio = new Servicio();
     *         List<Entidad> lista = servicio.getEntities();
     *         assertFalse(lista.isEmpty());
     *         Entidad primero = lista.get(0);
     *         assertNotNull(primero.getName(), "El nombre no debería ser nulo");
     *         assertNotNull(primero.getDescripcion(), "La descripción no debería ser nula");
     *         assertTrue(primero.getId() > 0, "El ID debería ser positivo");
     *     }
     */

}
